{
  "name": "josh-vibes-telegram-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "JOSH-VIBES Telegram personal AI assistant with knowledge base, lead capture, private mode, and provider failover.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "lint": "node --check src/index.js && node --check src/bot.js && node --check src/ai.js && node --check src/db.js && node --check src/knowledge.js && node --check src/config.js && node --check src/utils.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "grammy": "^1.35.0"
  }
}
